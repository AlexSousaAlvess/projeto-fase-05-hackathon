server:
  port: 8087

spring:
  application:
    name: sus-mock-service

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}

springdoc:
  swagger-ui:
    path: /swagger-ui.html
